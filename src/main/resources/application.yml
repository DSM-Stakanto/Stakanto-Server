spring:
  jpa:
    generate-ddl: false
    database: mysql
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
  datasource:
    url: ${URL}
    username: ${USERNAME}
    password: ${PASSWORD}
jwt:
  secret: ${SECRET}
  expired:
    access: ${ACCESS_TOKEN_EXPIRED}
    refresh: ${REFRESH_TOKEN_EXPIRED}
server:
  port: 8081